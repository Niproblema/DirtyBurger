import React from 'react';
import classes from './FoodTypeSelector.css'

const FoodTypeSelector = (props) => {
    return (
        <div className={classes.TypeSelector}>
            FoodTypeSelector
        </div>
    );
}

export default FoodTypeSelector;